package svgastronomie

// Below are constants used in the svgastronomie package.
const (
	BaseURL         string = "https://sv-gastronomie.de"
	MenuURL         string = BaseURL + "/menu"
	MenuTemplateURL string = BaseURL + "/menu/%s/%s" // %s is the restaurant URL
)
